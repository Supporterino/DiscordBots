{
  "name": "dcbot",
  "version": "1.0.0",
  "description": "Bot to move people around",
  "main": "src/bot.ts",
  "scripts": {
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/bot.js"
  },
  "author": "Supporterino",
  "license": "ISC",
  "bin": "build/bot.js",
  "dependencies": {
    "@types/node": "^14.0.5",
    "discord.js": "^12.2.0",
    "winston": "^3.2.1"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typedoc": "^0.17.7",
    "typescript": "^3.9.3"
  }
}
